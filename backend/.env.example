# Exemplo de variáveis de ambiente para envio de e-mail
# SMTP (ex: Gmail SMTP, serviços de provedor ou Mailgun/SendGrid via SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu_usuario_smtp
SMTP_PASS=sua_senha_smtp
FROM_EMAIL=seu@dominio.com
PORT=3001
